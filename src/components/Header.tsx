import React from 'react';

const Header = () => {
  return (
    <div
      style={{
        width: '100%',
        height: '70px',
        marginBottom: '50px',

        backgroundColor: 'blue',
      }}
    ></div>
  );
};

export default Header;
